<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.LokalJobApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your base theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>

        <item name="toolbarStyle">@style/ToolbarStyle</item>
    </style>

    <!-- Application theme extending from Base Theme -->
    <style name="Theme.LokalJobApp" parent="Base.Theme.LokalJobApp">
        <!-- Customize specific theme variations here -->
    </style>

    <style name="ToolbarStyle" parent="Widget.Material3.Toolbar">
        <item name="backgroundTint">@color/colorPrimary</item>
        <item name="titleTextColor">@color/colorOnPrimary</item>
    </style>
</resources>
